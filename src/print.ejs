<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF</title>

    <style>
        h1{
            width: 100vw;
            text-align: center;
        }
        
        table{
            width: 100%;
        }

        th{
            text-align: left;
        }

        tfoot tr td{
            font-size: 18px;
            font-weight: bold;
            text-align: center;
            width: 100%;
        }
    </style>

</head>
<body>
    <h1>Lista de passageiros</h1>
    <table>
        <thead>
            <tr>
                <th>Passageiro</th>
                <th>Numero do voo</th>
                <th>Horario</th>
            </tr>
        </thead>
        <tbody>
            <% if(passengers.length > 0) { %>
                <% passengers.forEach(passenger => { %>
                    <tr>
                        <td><%= passenger.name %></td>
                        <td><%= passenger.flightNumber %></td>
                        <td><%= passenger.time %></td>
                    </tr>
                <%  }); %>
            <% } %>
            </tbody>
            <tfoot>
                <tr><td colspan="2">Total de passageiros: <%=passengers.length%></td></tr>
            </tfoot>
    </table>
</body>
</html>